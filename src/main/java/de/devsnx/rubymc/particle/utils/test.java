package de.devsnx.rubymc.particle.utils;

import com.github.fierioziy.particlenativeapi.api.ParticleNativeAPI;
import com.github.fierioziy.particlenativeapi.core.ParticleNativeCore;
import de.devsnx.rubymc.particle.Particle;
import org.bukkit.Location;
import org.bukkit.entity.Player;

import java.util.Locale;

/**
 * @author DevSnx
 * @since 22.02.2023
 */
public class test {

    public void spawnCircle(Location location, Player player, int radius){

        ParticleNativeAPI particleApi = ParticleNativeCore.loadAPI(Particle.getInstance());

    }
}